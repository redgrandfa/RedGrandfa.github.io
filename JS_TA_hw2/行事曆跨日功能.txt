拖曳：
	只有圖片和超連結預設 draggable =true


1.draggable =true
2.dragstart 事件
3.事件中可以指定
	drag data	只有這項必須
	feedback image
	drag effect


(拖曳事件會向上傳遞)?
-------	drag data----------
所有拖曳事件都有一個屬性dataTransfer，是一個
DataTransfer物件，保有drag data

drag data指派：event.dataTransfer.setData(type,value)

----滑鼠座標-----
MouseEvent.唯讀屬性
pageX = 鼠標相對於app網頁檔的X座標
clientX = 鼠標相對於app的viewport的X座標(受到卷軸影響)(只有IE沒有?)
offsetX = 鼠標相對於事件源元素的X座標
screenX = 鼠標相對於顯示器的X座標


----offset-----
HTMLElement.
offsetWidth = 元素佔用寬度扣除水平外距占用的部分
(offsetHeight )

offsetLeft = 元素左上角相對於
	HTMLElement.offsetParent的左邊界的差
(offsetTop = ...上邊界)
offsetParent有可能null：應該是用viewport算

---座標找元素--------------

document.elementFromPoint(X,Y)	=> 取得HTMLElement


-----------------

local storag 資料結構

占用列怎處理?? 臨時計算? 記載著的話，受刪除影響
key:DateStart-single / Cross
value:[
	{
		dateEnd : 
		do : 
		type : ?
	}
]

取行程值 =easy  改localstorage =easy 
印行程=tdDate為關鍵字查找
早日優先放，晚日查找空位，class名預設， 
style絕對定位，相對於整列，寬度 left預定排位

跨日行程要有 假透明w1 遞減，以利查詢佔位

跨週做多個元素

區分refreshDate、refreshActivity

上個月延伸過來的? 如何顯示

===============================================
跨日拖曳可能性：
功能上：直接計算起點終點數字，放開才創建
視覺上：每列一個 DOM元素 疊在上層(position) ，計算長短?
insert before
----------------------
startCoord	滑鼠起點座標	
endCoord	滑鼠終點座標
(如果座標超出日曆主區，就取上/下限作為當前的X/Y)

tdWidth		=DOM.offsetWidth
tdHeight
startTd		起點格
endTd		終點格


定義頭尾：
先篩上下，若同列再篩左右

每排皆造一個div浮空，不需要顯示的，寬度為0
動態調整座標&寬度





(三)滑鼠放開，附著、推擠現有行程：
檢查各格是否有一個統一的空列
沒有就新增列

推擠 =每一格都要加一列 打底 (計算起點/終點格 之間的所有日期)
實際的跨日行程按鈕 z-index拉高
寬度得隨著變化...


(四)可拖曳至其他日
計算起終點格 => 增減此行程的始終日期 => 刷新顯示